apiVersion: v1
kind: Service
metadata:
  name: service-code-resolution-api
  namespace: default
  labels:
    app: service-code-resolution-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: service-code-resolution-api
